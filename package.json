{
  "name": "despoilerize",
  "version": "0.3.0",
  "private": true,
  "description": "Catch-up mode for hiding accidental sports result spoilers while browsing.",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && vite build && vite build --config vite.content.config.ts && node scripts/copy-static.mjs",
    "dev": "vite build --watch",
    "package:chrome": "npm run build && powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package-chrome.ps1",
    "promo:store": "node scripts/generate-store-promo-assets.mjs",
    "screenshots:store": "node scripts/generate-store-screenshots.mjs",
    "test:e2e": "npm run build && playwright test",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/chrome": "^0.0.327",
    "@types/node": "^24.0.0",
    "jsdom": "^26.0.0",
    "rimraf": "^6.0.0",
    "vitest": "^4.1.8"
  }
}
