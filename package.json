{
  "name": "despoilerze",
  "version": "0.1.0",
  "private": true,
  "description": "Catch-up mode for hiding accidental sports result spoilers while browsing.",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && vite build && node scripts/copy-static.mjs",
    "dev": "vite build --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.327",
    "jsdom": "^26.0.0",
    "rimraf": "^6.0.0",
    "vitest": "^4.1.8"
  }
}
